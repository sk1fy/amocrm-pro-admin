services:
  postgres:
    image: postgres:17-alpine
    environment:
      POSTGRES_DB: admin_test
      POSTGRES_USER: admin_test
      POSTGRES_PASSWORD: admin_test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U \"$$POSTGRES_USER\" -d \"$$POSTGRES_DB\""]
      interval: 1s
      timeout: 3s
      retries: 30
      start_period: 2s
    volumes:
      - postgres-test-data:/var/lib/postgresql/data
    networks:
      - integration-test

  migrate:
    image: amocrm-admin-migrate:integration-test
    build:
      context: ../backend
      dockerfile: Dockerfile
      target: migrate
      args:
        BUILD_REVISION: "${BUILD_REVISION:-unknown}"
        GO_VERSION: "${GO_VERSION:-1.25}"
    environment:
      DATABASE_URL: postgres://admin_test:admin_test@postgres:5432/admin_test?sslmode=disable
      MIGRATIONS_DIR: /migrations
      MIGRATION_TIMEOUT: 2m
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"
    networks:
      - integration-test

  integration-test:
    image: amocrm-admin-integration-test:local
    build:
      context: ../backend
      dockerfile: Dockerfile
      target: integration-test
      args:
        BUILD_REVISION: "${BUILD_REVISION:-unknown}"
        GO_VERSION: "${GO_VERSION:-1.25}"
    environment:
      TEST_DATABASE_URL: postgres://admin_test:admin_test@postgres:5432/admin_test?sslmode=disable
      TEST_DATABASE_RESET_ALLOWED: "true"
      MIGRATIONS_DIR: /src/migrations
    depends_on:
      postgres:
        condition: service_healthy
    restart: "no"
    networks:
      - integration-test

volumes:
  postgres-test-data:

networks:
  integration-test:
    driver: bridge
